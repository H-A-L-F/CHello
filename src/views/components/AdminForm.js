import { onSnapshot } from "firebase/firestore"
import { useEffect, useState } from "react"

function AdminForm() {
    const [members, setMembers] = useState()

    const adminCollectio

    useEffect(() => {
        const unsub = onSnapshot()
    }, [])

  return (
    <div>
        
    </div>
  )
}

export default AdminForm